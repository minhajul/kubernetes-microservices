#name: Deploy Laravel App
#
#on:
#  push:
#    branches:
#      - main
#
#env:
#  AWS_REGION: ap-southeast-1
#  ECR_REPOSITORY: laravel-app
#  PROJECT_DIR: /home/ec2-user/scalable-app
#
#jobs:
#  deploy:
#    name: Build, Push, and Deploy
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v3
#
#      # 1. Configure AWS credentials
#      - name: Configure AWS Credentials
#        uses: aws-actions/configure-aws-credentials@v4
#        with:
#          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#          aws-region: ${{ secrets.AWS_REGION }}
#
#      # 2. Log in to ECR and get repo URL
#      - name: Log in to Amazon ECR
#        id: login-ecr
#        uses: aws-actions/amazon-ecr-login@v2
#
#      # 3. Build, tag, and push Docker image
#      - name: Build and push Laravel image
#        id: build-image
#        env:
#          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
#          IMAGE_TAG: ${{ github.sha }}
#        run: |
#          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG -f laravel-app/Dockerfile ./laravel-app
#          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
#          # Store the full image URL for the deploy step
#          echo "::set-output name=image::$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG"
#
#      # 4. SSH into server and deploy
#      - name: Deploy to EC2
#        uses: appleboy/ssh-action@master
#        with:
#          host: ${{ secrets.EC2_HOST_DNS }}
#          username: ec2-user
#          key: ${{ secrets.EC2_SSH_KEY }}
#          script: |
#            # 1. Clone or pull the repo
#            if [ -d ${{ env.PROJECT_DIR }} ]; then
#              cd ${{ env.PROJECT_DIR }}
#              git pull
#            else
#              git clone https://github.com/${{ github.repository }}.git ${{ env.PROJECT_DIR }}
#              cd ${{ env.PROJECT_DIR }}
#            fi
#
#            # 2. Log in to ECR (EC2 instance needs IAM role for this)
#            # Make sure your EC2 instance has an IAM Role attached
#            # that allows ECR actions (e.g., AmazonEC2ContainerRegistryReadOnly)
#            sudo aws ecr get-login-password --region ${{ secrets.AWS_REGION }} | sudo docker login --username AWS --password-stdin ${{ steps.login-ecr.outputs.registry }}
#
#            # 3. Update the image reference in docker-compose.yml
#            # This replaces the 'build' section with the new image
#            sed -i "s|build:.*|image: ${{ steps.build-image.outputs.image }}|g" docker-compose.yml
#            sed -i "/context:/d" docker-compose.yml
#            sed -i "/dockerfile:/d" docker-compose.yml
#
#            # 4. Pull the new laravel-app image
#            sudo /usr/local/bin/docker-compose pull laravel-app
#
#            # 5. Restart all services with the new image
#            sudo /usr/local/bin/docker-compose up -d